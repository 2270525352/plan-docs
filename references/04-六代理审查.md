# 有预算的六代理开发就绪审查

审查目标是判断文档能否安全驱动开发，不是追求措辞、格式或理论完整性。先运行确定性审计，
协调者处理机械错误并冻结准备审查的规划；完整六代理审查默认只运行一次。

## 隔离原则

六个代理分别在干净上下文中读取原始项目文档，不共享其他审查代理的结论，也不接收“预期答案”或已知缺陷提示。它们只能把各轮原始报告写入 `06-reviews/agents/<review_round>-<reviewer_id>.md` 或返回报告文本，不能修改用户原话、规划或代码。

协调者逐个分发精确报告路径，并同时在 `审查分发与写锁.md` 与 `CURRENT_STATE.md`
登记当前 Reviewer、TASK、唯一 raw 报告锁；未激活精确锁的 Reviewer 必须停止且不创建
报告。每次激活还必须登记外部调度器返回的唯一 run/thread ID、随机 dispatch nonce，以及
六个 Reviewer 共用的 planning source snapshot SHA-256；报告内必须逐字段回传。A1 只能写
A1 报告，依此类推；代理禁止读取同轮其他报告、汇总和门禁。

Reviewer 写完只返回 `submitted` 并退出，不修改状态。进程结束后，协调者可执行一次仅限锁
交接的控制面转换：校验 schema，记录 SHA-256/bytes，把报告标为 `immutable`，再激活下一
Reviewer。转换不得修改 raw 报告或其他规划内容。已存在路径不能覆盖，失败重跑使用新
review round/path。协调者最终只写 `审查汇总.md` 与 `自动模式门禁.md`。

独立 Reviewer 不遵循普通窗口的完整状态读取流程。preflight 只提取
`CURRENT_STATE.md` 的 `## Current snapshot` 有界区块（到下一个二级标题为止）和分发表中
自己的单行；不得读取状态日志、历史审查摘要、旧 raw 报告或其他 Reviewer 行。误读即污染，
必须零写入停止并换新上下文。

## 分工

| 代理 | 审查范围 | 必须给出的证据 |
|---|---|---|
| A1 | 用户原话 ↔ AI 可读需求 | U/REQ 缺失或误读映射 |
| A2 | 遗漏、歧义、假设、验收 | Q/REQ 与不可判定标准 |
| A3 | 架构、接口、数据流、模块 | ADR/API/MOD 覆盖与断链 |
| A4 | 总任务、依赖、并行、文件冲突 | TASK、路径、锁、拓扑 |
| A5 | Claude/Codex/Reviewer/OpenCode 边界 | owner、禁止范围、职责冲突 |
| A6 | 测试、Git、反馈、自动化、停止条件 | TEST/FB/CP/automation/gate |

## 统一报告格式

使用唯一模板 [代理审查报告.md](../templates/fragments/代理审查报告.md)。每个 finding
必须以 `### REV-A1-001` 这类 Markdown 标题开始，标题下保留同值的 `finding_id`、
`severity`、`evidence`、`affected_ids`、`problem`、`required_resolution` 与 `status`
字段；审计脚本据此核对原始报告和汇总，不能改成自由文本或 YAML 列表。

每个 finding 还必须填写：

- `development_impact`：会怎样影响实现、验收、安全或兼容性；
- `blocking_task_ids`：实际被阻塞的 `TASK-*`，没有则使用 `[]`；
- `affected_paths`：需要修改或验证的文件范围。

无法给出具体开发影响和阻塞任务的 finding 只能标为 P2。

分级：

- GREEN：范围完整、可追溯、无阻塞；
- YELLOW：存在缺口、假设或弱验证；标注 P0/P1/P2；
- RED：与原话冲突、需求/任务缺失、文件冲突、职责混淆或无法安全执行。

P2 进入 `p2_backlog`，不阻塞门禁。禁止为了关闭 P2 自动修改稳定规划或触发新的完整审查。

## 汇总与复审

协调者：

1. 收集六份原始报告；
2. 去重但保留所有 finding ID 和证据；
3. 把问题分配给相应规划文档，而非静默修复；
4. 更新 `审查汇总.md` 和 `CURRENT_STATE.md`；
5. 修订需求、产品、架构、任务或分工；
6. 只对实际变更范围运行对应代理；不得把定向复审扩大成默认全量复审；
7. 保留前后审查记录，不覆盖旧结论。

默认预算：

- 完整六代理审查：最多 1 轮；
- 定向复审：最多 1 轮；
- Reviewer 总调用：最多 12 次；
- 超出任一上限：门禁保持 `BLOCKED`，记录当前证据并取得新的用户原话授权；不得自动循环。

若第一次完整审查发现 P0/P1，协调者可以修订并执行一次定向复审。若修订改变了定向 Reviewer
之外的事实域，或当前审计要求新的完整快照证据，则停止并让用户决定是否批准额外完整轮次。

## 自动模式门禁

门禁只有在以下条件全部满足时为 READY：

- 六个代理均有最新报告；
- 无 RED；
- 无未关闭的 P0/P1 YELLOW；
- P2 已进入非阻塞待办；
- 审查轮次和 Reviewer 调用未超过确认预算；
- 每个 REQ 有 TASK 和 acceptance；
- 每个 TASK 有 owner、范围、验证、测试、锁、反馈、停止条件；
- 写锁和接口锁无冲突；
- 分工与 Git 策略已确认；
- 用户确认最终规划和分工。

否则结论为 BLOCKED，并列出 `finding_id → owner → 修订文档 → 复审代理`。

## 并发降级

若平台不能并行六个代理：

1. 启动六次新的独立上下文；
2. 每次只给同一份审查提示词和一个 reviewer_id；
3. 清除上一次上下文或新建会话；
4. 直到六份报告齐全再汇总；
5. 在报告中记录 `context_isolation: degraded` 及原因。
