# 任务 I/O 合同注册表

本文件是 `input_contracts` / `output_contracts` 的唯一事实来源。合同 ID 不能只有名称；
必须定义可定位制品、结构、完成证据和生产/消费双方。

## CONTRACT-001

contract_id: CONTRACT-001

producer_tasks: []

consumer_tasks: []

artifact_refs: []

required_content:

completion_condition:

verification:

compatibility:

status: draft / frozen / deprecated
