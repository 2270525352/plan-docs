# 无 CCB 多窗口协作模式

coordinator:

merge_authority:

每个窗口先读 `AGENTS.md`、`CURRENT_STATE.md` 和自己的任务文档。写入前取得文件锁并重读磁盘最新版；不得写另一个窗口锁定的文件。共享接口由协调者冻结，消费者只依赖已 checkpoint 的版本。所有完成、阻塞、交接和 commit 都回写 `CURRENT_STATE.md`。
